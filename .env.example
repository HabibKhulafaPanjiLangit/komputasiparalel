# Environment variables untuk database
# Copy file ini ke .env dan isi dengan credentials database Anda

# Local development (gunakan SQLite, tidak perlu diisi)
# DATABASE_URL akan otomatis menggunakan sqlite:///payroll.db

# Production (Railway/Render akan auto-set ini)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Flask settings
FLASK_ENV=production
SECRET_KEY=your-secret-key-here-change-in-production

# App settings
PORT=5000
